# PixelCraft Studio — คู่มือสำหรับทีม

อ่านอันนี้ก่อนเริ่มแก้โค้ด จะได้ไม่ต้องเขียน CSS ใหม่ และไม่ชนกันตอน push

---

## 1. ใครแก้ไฟล์ไหน

| ไฟล์ | ใครดูแล | แก้อะไร |
|---|---|---|
| `home-v2.html` → section `#services` | **เพื่อนคนที่ 1** | เนื้อหา "บริษัททำอะไรบ้าง" |
| `profile-v2.html` → section `#team` | **เพื่อนคนที่ 2** | การ์ดแนะนำสมาชิก 3 คน |
| ทุกอย่างที่เหลือ + `assets/` | คนที่วางโครง | โครงหน้า, ระบบดีไซน์ |

**กติกาข้อเดียว:** อยู่ใน section ของตัวเอง ถ้าจำเป็นต้องแตะ `assets/css/base.css`
บอกกันในกลุ่มก่อน เพราะไฟล์นั้นใช้ร่วมกันทั้งเว็บ แก้ผิดพังทั้งสองหน้า

---

## 2. โครงสร้างไฟล์

```
JavaGroupProject/
├── home-v2.html          หน้าแรก
├── profile-v2.html       หน้าทีม
├── STYLEGUIDE.md         ไฟล์นี้
├── assets/
│   ├── css/
│   │   ├── base.css      ใช้ร่วมกัน: สี ฟอนต์ ปุ่ม navbar footer status bar
│   │   ├── home.css      เฉพาะหน้าแรก (มีคลาส .svc ของ services รออยู่)
│   │   └── profile.css   เฉพาะหน้าทีม (มีคลาส .mate ของสมาชิกรออยู่)
│   └── js/
│       └── site.js       ใช้ร่วมกันทั้งสองหน้า
└── portfolio/
    ├── Aom/              เสร็จแล้ว
    ├── Jeff/             ยังว่าง
    └── Mon/              ยังว่าง
```

`base.css` ต้องโหลดก่อน `home.css` / `profile.css` เสมอ — ใน `<head>` เรียงไว้ให้แล้ว

---

## 3. ของกลางที่หยิบใช้ได้เลย

### สี — เรียกด้วย `var(...)` อย่าพิมพ์รหัสสีตรงๆ

| ตัวแปร | สี | ใช้ตอนไหน |
|---|---|---|
| `--ink` | ดำอมน้ำเงิน | ตัวหนังสือ เส้นขอบ เงา |
| `--foil` | เงินอ่อน | พื้นหลัง |
| `--steel` | เทา | ตัวหนังสือรองบนพื้นเข้ม |
| `--lime` | เขียวไลม์ | **Aom** · ปุ่มหลัก |
| `--plasma` | น้ำเงินสด | **Jeff** · ลิงก์ โครงสร้าง |
| `--hyper` | ชมพูสด | **Mon** · selection, focus |

สามสีท้ายไม่ได้มีไว้สวยอย่างเดียว — มันบอกว่าใครเป็นเจ้าของอะไร
เคอร์เซอร์ในหน้าแรกกับการ์ดสมาชิกในหน้าทีมต้องใช้สีตรงกัน

### ฟอนต์

| ตัวแปร | ฟอนต์ | ใช้กับ |
|---|---|---|
| `--f-disp` | Archivo | หัวข้อใหญ่ ชื่อ ตัวเลข |
| `--f-body` | Schibsted Grotesk | เนื้อหา |
| `--f-mono` | Space Mono | ป้ายกำกับเล็กๆ ทุกอัน |

### ปุ่ม

```html
<a class="btn btn--acid" href="#">ปุ่มหลัก สีไลม์</a>
<a class="btn btn--chrome" href="#">ปุ่มรอง โครมเมียม</a>
<a class="btn btn--onink" href="#">ปุ่มสำหรับวางบนพื้นสีเข้ม</a>
<a class="btn btn--c" href="#">ปุ่มสีตามตัวแปร --c ของตัวแม่</a>
```

ใส่ลูกศรท้ายปุ่มด้วย `<span class="btn__x" aria-hidden="true">→</span>`
วางปุ่มหลายอันเรียงกันให้ครอบด้วย `<div class="btn-row">`

### หัวข้อ section

```html
<div class="head">
  <h2 class="h2">หัวข้อ<br>ขึ้นบรรทัดใหม่ได้</h2>
  <p class="head__note">คำอธิบายสั้นๆ ไปอยู่ขวามือ</p>
</div>
```

---

## 4. งานของเพื่อนคนที่ 1 — Services

**ไฟล์:** `home-v2.html` · หา `<section class="band ink" id="services">`
**ทำอะไร:** ลบ `<div class="blank">` ทิ้ง แล้วใส่กริดการ์ดแทน

### คลาสที่มีให้แล้วใน `assets/css/home.css`

| คลาส | คืออะไร |
|---|---|
| `.svcs` | กล่องกริด ครอบการ์ดทั้งหมด เพิ่มการ์ดกี่ใบก็จัดให้เอง |
| `.svc` | การ์ด 1 ใบ ตั้งสีแถบบนด้วย `style="--c:..."` |
| `.svc__tag` | ป้ายเล็กมุมบนการ์ด |
| `.svc__t` | ชื่อบริการ |
| `.svc__d` | คำอธิบาย |
| `.svc__hint` | บรรทัดล่างสุด ชิดก้นการ์ดเสมอ ต่อให้ข้อความยาวไม่เท่ากัน |

### โค้ดตั้งต้น

```html
<div class="svcs">

  <article class="svc" style="--c:var(--lime)">
    <span class="svc__tag">ป้ายสั้นๆ</span>
    <h3 class="svc__t">ชื่อบริการ</h3>
    <p class="svc__d">อธิบายว่าลูกค้าจะได้อะไร 1–2 บรรทัด</p>
    <p class="svc__hint">เครื่องมือ · ที่ · ใช้</p>
  </article>

  <!-- ก๊อป <article> ข้างบนไปเพิ่ม แล้วเปลี่ยน --c เป็นสีอื่น -->

</div>
```

สีแถบบนที่ใช้ได้: `var(--lime)` `var(--plasma)` `var(--hyper)` `#F4F7FC`
ถ้าทำ 4 ใบ ใช้ครบทั้ง 4 สีจะสวยที่สุด

### วัตถุดิบ

เนื้อหาที่บริษัททำมีอยู่ใน `Readme.MD` แล้ว — ออกแบบเว็บ, UI/UX,
responsive, brand identity, e-commerce เลือกมา 4 อย่างก็พอ

**เขียนยังไงให้ไม่เหมือนเว็บทั่วไป:** บอกว่าลูกค้า *ได้อะไรกลับไป*
ไม่ใช่บอกว่าเราเก่งอะไร — "ได้เว็บที่ใช้งานได้จริง ไม่ใช่รูปเว็บ"
ดีกว่า "เราเชี่ยวชาญการออกแบบเว็บระดับมืออาชีพ"

---

## 5. งานของเพื่อนคนที่ 2 — Team members

**ไฟล์:** `profile-v2.html` · หา `<section class="band" id="team">`
**ทำอะไร:** ลบ `<div class="blank">` ทิ้ง แล้วใส่โครง `.team` แทน

### คลาสที่มีให้แล้วใน `assets/css/profile.css`

| คลาส | คืออะไร |
|---|---|
| `.team` | กริด 2 คอลัมน์: แผงรายชื่อซ้าย + การ์ดขวา |
| `.layers` | แผงรายชื่อ ติดหนึบตามตอนเลื่อนหน้า |
| `.mates` | กองการ์ดสมาชิก |
| `.mate` | การ์ด 1 คน ตั้งสีประจำตัวที่นี่ |
| `.mate__sel` | กรอบ selection รอบการ์ด (ต้องมี `<i>` 4 อันข้างใน) |
| `.mate__tag` | ป้ายชื่อมุมบนซ้าย |
| `.mate__in` | กล่องเนื้อหา รูปซ้าย ข้อความขวา |
| `.portrait` | แผ่นโครมเมียมแทนรูปถ่าย |
| `.mate__name` `.mate__role` `.mate__d` | ชื่อ / ตำแหน่ง / คำอธิบาย |
| `.kit` | แถวป้ายทักษะ ใช้ `<ul>` กับ `<li>` |
| `.stat` | บรรทัดสถานะเล็กๆ ข้างปุ่ม |

### สีประจำตัว — ต้องตรงกับเคอร์เซอร์ในหน้าแรก

| คน | ใส่ที่ `.mate` และ `.layers a` |
|---|---|
| Aom | `style="--c:var(--lime); --on:#0C0D14"` |
| Jeff | `style="--c:var(--plasma); --on:#fff"` |
| Mon | `style="--c:var(--hyper); --on:#fff"` |

`--c` คือสีประจำตัว · `--on` คือสีตัวหนังสือที่วางบนสีนั้นแล้วยังอ่านออก

### โค้ดตั้งต้น

```html
<div class="team">

  <nav class="layers" aria-label="Team members">
    <p class="layers__t">Layers <span>3</span></p>
    <a href="#aom" data-layer="aom" style="--c:var(--lime); --on:#0C0D14">Aom<em>ตำแหน่งย่อ</em></a>
    <!-- เพิ่ม Jeff กับ Mon ต่อ -->
    <p class="layers__foot">All visible · locked 0</p>
  </nav>

  <div class="mates">

    <article class="mate" id="aom" style="--c:var(--lime); --on:#0C0D14">
      <span class="mate__sel" aria-hidden="true"><i></i><i></i><i></i><i></i></span>
      <span class="mate__tag" aria-hidden="true">Aom · editing</span>

      <div class="mate__in">
        <div class="portrait">
          <span class="portrait__ltr" aria-hidden="true">A</span>
          <span class="portrait__meta"><span>Layer 01</span><b>ตำแหน่งย่อ</b></span>
        </div>

        <div>
          <h3 class="mate__name">Aom</h3>
          <p class="mate__role">ตำแหน่ง · ประสบการณ์กี่ปี</p>
          <p class="mate__d">คนนี้ทำอะไรในทีม 2–3 บรรทัด</p>
          <ul class="kit">
            <li>HTML</li><li>CSS</li><li>JavaScript</li>
          </ul>
          <div class="mate__go">
            <a class="btn btn--c" href="portfolio/Aom/index.html">Open portfolio
              <span class="btn__x" aria-hidden="true">→</span></a>
            <span class="stat"><span class="pip pip--lime" aria-hidden="true"></span> Portfolio live</span>
          </div>
        </div>
      </div>
    </article>

    <!-- ก๊อป <article> ข้างบนไปทำของ Jeff (id="jeff") กับ Mon (id="mon") -->

  </div>
</div>
```

### สองอย่างที่พลาดบ่อย

1. **`id` ของ `.mate` ต้องตรงกับ `data-layer` ใน `.layers`** (`aom` / `jeff` / `mon`)
   ไม่งั้นแผงรายชื่อจะไม่ไฮไลต์ตามตอนเลื่อนหน้า — `site.js` จับคู่จากตรงนี้
2. **ถ้ามีรูปถ่ายจริง** เอา `<img>` ใส่ใน `.portrait` แทน `.portrait__ltr` ได้เลย
   กรอบกับแถบล่างยังใช้ได้เหมือนเดิม

### วัตถุดิบ

ข้อมูลสมาชิกอยู่ใน `Readme.MD` — ตำแหน่ง ประสบการณ์ ทักษะ ครบแล้ว
`portfolio/Jeff/` กับ `portfolio/Mon/` ยังว่าง ลิงก์เตรียมไว้แล้วในโค้ด
พอมี `index.html` ลงไปลิงก์จะใช้ได้ทันที ระหว่างนี้ใส่ `.stat` ว่า
"Portfolio in progress" ไปก่อน

---

## 6. งานของเจ้าของโครง — Work accordion (ทำเสร็จแล้ว)

**ไฟล์:** `home-v2.html` · `<section class="band band--work" id="work">`
ข้อมูลมาจาก `COMPANY_PROJECTS.md` เนื้อหาข้างในเป็นภาษาไทย

### โครงหนึ่งการ์ด

```html
<article class="row" style="--c:var(--plasma); --on:#fff; --cd:#7C7CFF">
  <h3 class="row__h">
    <button class="row__btn" type="button" id="wb-xxx"
            aria-expanded="true" aria-controls="wp-xxx">
      <span class="row__idx">01</span>
      <span class="row__name">ชื่ออังกฤษ
        <span class="row__th">ชื่อไทย</span>
      </span>
      <span class="row__cat">หมวดหมู่</span>
      <span class="row__sign" aria-hidden="true"></span>
    </button>
  </h3>

  <div class="row__panel" id="wp-xxx" role="region" aria-labelledby="wb-xxx">
    <div class="row__panelin">
      <div class="pan">
        <div class="fact"><p class="fact__k">Client</p><p class="fact__v">…</p></div>
        <div class="pan__two">
          <div class="fact"><p class="fact__k">The problem</p><p class="fact__v">…</p></div>
          <div class="fact"><p class="fact__k">What we built</p>
            <ul class="solu"><li><b>ชื่อระบบ</b> คำอธิบาย</li></ul>
          </div>
        </div>
        <div class="fact"><p class="fact__k">Results</p>
          <div class="mets">
            <div class="met">
              <p class="met__v">ตัวเลข</p>
              <p class="met__u">หน่วย</p>
              <p class="met__d">ขยายความ</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</article>
```

### ตัวแปรสีสามตัว

| ตัวแปร | ใช้ตรงไหน |
|---|---|
| `--c` | แถบซ้าย + พื้นหัวการ์ดตอนกางออก |
| `--on` | สีตัวหนังสือบนพื้น `--c` |
| `--cd` | สีเดียวกันเวอร์ชันสว่าง สำหรับใช้บนพื้นดำในแผงรายละเอียด |

**ทำไมต้องมี `--cd`:** สีน้ำเงิน `--plasma` (#2B2BFF) วางบนพื้นดำแล้วมืดจนอ่านไม่ออก
เลยต้องมีคู่สว่างไว้ใช้เฉพาะในแผง สีไลม์ไม่ต้องมี ใช้ `var(--lime)` ทั้งสองที่ได้เลย

### สิ่งที่ต้องระวังถ้าจะแก้

1. **`id` ของปุ่มกับแผงต้องจับคู่กัน** — `aria-controls` ของปุ่มชี้ไปที่ `id` ของแผง
   และ `aria-labelledby` ของแผงชี้กลับมาที่ `id` ของปุ่ม ถ้าไม่ตรง screen reader จะอ่านมั่ว
2. **`aria-expanded="true"` ใน HTML ไม่ใช่ความผิดพลาด** — ตั้งใจให้กางไว้ตั้งแต่แรก
   คนที่ปิด JS จะได้อ่านผลงานครบทุกชิ้น `site.js` จะหุบให้เองตอนโหลดเสร็จ
   ถ้าเขียนกลับกันเป็น `false` คนที่ JS ไม่ทำงานจะไม่เห็นผลงานเลยสักชิ้น
3. **การกางใช้ `grid-template-rows: 0fr → 1fr`** ไม่ใช่ `max-height` เพราะแบบนี้
   ความสูงพอดีเนื้อหาจริงเสมอ ไม่ต้องเดาตัวเลข และไม่มีอาการกระตุกตอนจบ
4. **เปิดได้ทีละอัน** — กดอันใหม่ อันเก่าจะหุบเอง กดอันเดิมซ้ำคือปิด
5. **คีย์บอร์ด** — Tab ไปที่หัวการ์ด, Enter/Space กางหรือหุบ, ลูกศรขึ้น-ลงเลื่อนระหว่างการ์ด,
   Home/End ไปอันแรก-อันสุดท้าย

---

## 7. ภาษาไทยบนหน้าเว็บ

ฟอนต์หลักสามตัว (Archivo / Schibsted Grotesk / Space Mono) **ไม่มีสระไทย**
ถ้าใส่ตัวไทยลงไปเฉยๆ เบราว์เซอร์จะไปหยิบฟอนต์ระบบมาแทน หน้าตาจะคนละเรื่องกันทั้งเว็บ

แก้แล้วด้วยการใส่ **IBM Plex Sans Thai** เป็นตัวสำรองในทุกกองฟอนต์ใน `base.css`
เบราว์เซอร์เลือกฟอนต์ทีละตัวอักษร ตัวอังกฤษเลยได้ฟอนต์แรก ส่วนตัวไทยตกมาใช้ตัวไทย
**ไม่ต้องทำอะไรเพิ่ม** พิมพ์ไทยลงไปได้เลย

ข้อควรระวังเวลาเขียนไทย:

- **ระยะบรรทัดต้องมากกว่าอังกฤษ** ไทยมีสระบนกับสระล่าง ถ้าใช้ `line-height` 1.4
  สระของสองบรรทัดจะชนกัน ในแผงรายละเอียดตั้งไว้ที่ 1.8–1.85 แล้ว
- **อย่าใส่ `letter-spacing` กับตัวไทย** มันจะดันสระออกจากตัวอักษร คลาส `.row__th`
  เลยรีเซ็ต `letter-spacing: 0` ทิ้งไว้ ทั้งที่ตัวแม่มันตั้งไว้
- **ป้ายกำกับตัวเล็ก (`.fact__k`) ยังเป็นอังกฤษ** เพราะฟอนต์ mono กับ letter-spacing
  กว้างๆ เข้ากับตัวไทยไม่ได้ — เนื้อหาเป็นไทย ป้ายกำกับเป็นอังกฤษ เป็นรูปแบบปกติ

---

## 8. กฎ 5 ข้อที่อย่าทำพัง

1. **อย่าลบกรอบ focus** — `:focus-visible` ใน `base.css` คือสิ่งเดียวที่บอกคนใช้คีย์บอร์ดว่าตอนนี้อยู่ตรงไหน
2. **อย่าใส่ `!important`** ถ้าสไตล์ไม่ติด แปลว่าใส่ class ผิด ไม่ใช่ต้องเพิ่มน้ำหนัก
3. **อย่าพิมพ์รหัสสีตรงๆ** ใช้ `var(--lime)` ไม่ใช่ `#CBFF2E` — วันหลังเปลี่ยนสีทั้งเว็บที่เดียวจบ
4. **เช็คจอมือถือทุกครั้ง** ย่อหน้าต่างเบราว์เซอร์ให้แคบสุด ต้องไม่มีแถบเลื่อนแนวนอนโผล่มา
5. **ตัวหนังสือโครมเมียม** ถ้าเปลี่ยนคำ ต้องแก้ทั้ง `data-word` และข้างใน `<span>` ให้ตรงกัน แล้วปรับ `font-size` ใน CSS ด้วย เพราะคำยาวไม่เท่ากัน

---

## 9. ทดสอบก่อน push

- เปิดทั้งสองหน้า กดลิงก์ทุกอันดูว่าไปถูกที่
- กดการ์ดผลงานทุกใบ ต้องกางและหุบได้ เปิดได้ทีละอัน
- ย่อหน้าต่างจนแคบสุด → ต้องไม่มีแถบเลื่อนแนวนอน เมนูต้องกลายเป็นปุ่มขีดสามขีด
- กด `Tab` รัวๆ → ต้องเห็นกรอบชมพูวิ่งไปทีละอัน ไม่หายไปไหน
- เปิด Console (F12) → ต้องไม่มีข้อความแดง

---

## 10. คอนเซปต์ของงาน (เผื่ออาจารย์ถาม)

ทั้งเว็บทำตัวเหมือน **ไฟล์ดีไซน์ที่เปิดค้างอยู่** ไม่ใช่เว็บบริษัททั่วไป —
แถบบนบอกชื่อไฟล์ที่กำลังเปิด แถบล่างเป็น status bar หน้าแรกมีกรอบ selection
รอบชื่อบริษัทพร้อมตัวเลขขนาดจริง และมีเคอร์เซอร์ของสมาชิกสามคนลอยอยู่
เหมือนกำลังแก้ไฟล์ด้วยกัน หน้าทีมก็เอาแนวคิดเดียวกันมาต่อ — สมาชิกแต่ละคน
คือ object ที่ถูกเลือกอยู่บน canvas

สไตล์: **Y2K chrome / acid maximal** โครมเมียม + สีจัดจ้าน + ขอบดำหนา + เงาแข็ง
